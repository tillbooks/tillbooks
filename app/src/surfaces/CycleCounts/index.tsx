/**
 * Route entry for the Inventory -> Cycle Counts / Inventur surface (J04). Default export so the router
 * wires `/cycle-counts` in one line. Co-located CSS imported here so the styles load with the surface.
 */
import './CycleCounts.css';
import { CycleCounts } from './CycleCounts';

export default CycleCounts;
export { CycleCounts };
