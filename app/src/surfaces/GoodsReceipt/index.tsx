/**
 * Route entry for the Einkauf -> Wareneingänge surface (I02). Default export so the router wires
 * `/goods-receipts` in one line. Co-located CSS imported here so the styles load with the surface.
 */
import './GoodsReceipt.css';
import { GoodsReceipt } from './GoodsReceipt';

export default GoodsReceipt;
export { GoodsReceipt };
