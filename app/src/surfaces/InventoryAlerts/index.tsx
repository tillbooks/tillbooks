/**
 * Route entry for Lager -> Bestand & Alarme (J07). Default export so the router wires
 * `/inventory-alerts` in one line.
 */
import { InventoryAlerts } from './InventoryAlerts';

export default InventoryAlerts;
export { InventoryAlerts };
