/**
 * Route entry for the Inventory -> Movements surface (J02). Default export so the router wires
 * `/inventory-movements` in one line. Co-located CSS imported here so the styles load with the surface.
 */
import './InventoryMovements.css';
import { InventoryMovements } from './InventoryMovements';

export default InventoryMovements;
export { InventoryMovements };
