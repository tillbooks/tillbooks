/**
 * Route entry for the Inventory -> Bewertung surface (J03). Default export so the router wires
 * `/inventory-valuation` in one line. Co-located CSS imported here so the styles load with the surface.
 */
import './InventoryValuation.css';
import { InventoryValuation } from './InventoryValuation';

export default InventoryValuation;
export { InventoryValuation };
