/**
 * Route entry for the Einkauf -> Landed Costs surface (I03). Default export so the router wires
 * `/landed-costs` in one line. Co-located CSS imported here so the styles load with the surface.
 */
import './LandedCosts.css';
import { LandedCosts } from './LandedCosts';

export default LandedCosts;
export { LandedCosts };
