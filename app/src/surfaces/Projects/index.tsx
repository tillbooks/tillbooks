/**
 * The Projekte surface (B00): default export for the router, styles included at the entry so the
 * route carries its own CSS (the sibling surfaces' shape).
 */
import './Projects.css';
import { Projects } from './Projects';

export default Projects;
