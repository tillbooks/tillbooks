/**
 * Route entry for the Inventory -> Reason Codes / Korrekturgründe surface (J05). Default export so the
 * router wires `/inventory-reason-codes` in one line. Co-located CSS imported here so the styles load
 * with the surface.
 */
import './ReasonCodes.css';
import { ReasonCodes } from './ReasonCodes';

export default ReasonCodes;
export { ReasonCodes };
