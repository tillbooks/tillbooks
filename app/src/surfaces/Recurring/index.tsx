/** Public entry point for the A12 Serien surface, wired at `/recurring` in `router.tsx`. */
export { Recurring, default } from './Recurring';
export type { ScheduleDto, ScheduleLineDto, RunDto } from './Recurring';
