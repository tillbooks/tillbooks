/**
 * Route entry for the Einkauf -> Anforderungen surface (I00, requisitions).
 *
 * Default export so the router wires `/requisitions` in one line. Co-located CSS imported here so the
 * styles load with the surface.
 */
import './Requisitions.css';
import { Requisitions } from './Requisitions';

export default Requisitions;
export { Requisitions };
