/** Public entry point for the G07 Suche surface, wired at `/search` in `router.tsx`; the palette
 *  overlay (`SearchPalette`) mounts once in `Shell.tsx` and opens on ⌘K / Ctrl+K. */
export { Search, default } from './Search';
export { SearchPalette } from './SearchPalette';
