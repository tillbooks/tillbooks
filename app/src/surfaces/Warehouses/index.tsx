/**
 * Route entry for the Inventory -> Warehouses & Locations surface (J00). Default export so the router
 * wires `/warehouses` in one line. Co-located CSS imported here so the styles load with the surface.
 */
import './Warehouses.css';
import { Warehouses } from './Warehouses';

export default Warehouses;
export { Warehouses };
